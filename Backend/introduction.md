├── /config        # Database connection, environment settings [2]
├── /controllers   # Request handling logic, input validation [7, 15]
├── /models        # Database schemas (e.g., Mongoose, Sequelize) [2, 4]
├── /routes        # API route definitions (endpoint mappings) [2, 7]
├── /middlewares   # Custom auth or request-processing functions [13]
├── /utils         # Reusable helper functions/utilities [1]
├── /tests         # Unit and integration test files [4]
├── .env           # Sensitive data (API keys, DB URLs) [4]
├── .gitignore     # Files to ignore (e.g., node_modules) [15]
├── package.json   # Project dependencies and metadata [2, 15]
└── server.js      # Main entry point (app initialization) [15]
